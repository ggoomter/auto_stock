==========================================
🔄 백엔드 재시작이 필요합니다
==========================================

이벤트 라우터를 추가했으므로 백엔드 서버를 재시작해야 합니다.

Windows:
--------
1. 현재 실행 중인 백엔드 종료 (Ctrl+C)
2. run_backend.bat 더블클릭
   또는
   cd backend
   python -m uvicorn app.main:app --reload

Linux/Mac:
----------
1. 현재 실행 중인 백엔드 종료 (Ctrl+C)
2. ./run_backend.sh
   또는
   cd backend
   source venv/bin/activate
   uvicorn app.main:app --reload

재시작 후:
-----------
✅ http://localhost:8000/events/update/manual 엔드포인트 사용 가능
✅ 프론트엔드에서 "뉴스 수집" 버튼 작동

확인 방법:
----------
브라우저에서 http://localhost:8000/docs 접속
→ "Events" 섹션이 보이면 성공!

==========================================
