# Core dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.10.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
httpx>=0.26.0
requests>=2.31.0
apscheduler>=3.10.0

# Data science stack
# Python 3.14 사용 시: pip install --only-binary :all: 옵션 필요
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0

# Technical analysis
# Python 3.11: pandas-ta-classic (community fork)
# Python 3.12+: pandas-ta (official, requires Python 3.12+)
pandas-ta-classic>=0.3.37

# Finance data
yfinance>=0.2.50
finance-datareader>=0.9.96
pykrx>=1.2.8  # 한국거래소 주식 데이터 — 시총/펀더멘털 벌크 조회는 KRX 계정 필요 (backend/.env의 KRX_ID/KRX_PW)

# Security
python-jose[cryptography]>=3.3.0

# pykrx가 pkg_resources에 의존 — setuptools 81+에서 제거되어 크래시 (2026-07-05 확인)
setuptools<81

# 뉴스 크롤링 (venv에 이미 설치됨 — 선언 누락 보완)
beautifulsoup4>=4.12
lxml>=5.0
